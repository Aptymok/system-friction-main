---
title: "Patrones → MIHM"
description: "Mapa de patrones estructurales a variables MIHM. El puente entre observación cualitativa y diagnóstico cuantitativo."
permalink: /mihm/patrones/
nav: mihm
---

# Patrones → Variables MIHM

Cada documento de la serie describe un patrón observable.  
Este mapa conecta cada patrón con la variable MIHM que modifica y el nodo donde se observó.

El orden no es jerárquico. Es acumulativo.

---

## Mapa completo

<div id="sf-pattern-bridge" data-sf-load="true">—</div>

---

## Lectura del mapa

**friction_contribution** indica la magnitud del impacto en $f = (t/T) + O$:

- `critical` — incremento directo en $E_i$ o $L_i$ > 0.20
- `high` — incremento en $O$ o degradación de $M_i$ > 0.15
- `medium` — reducción de $C_i$ o $K_i$ sin colapso inmediato
- `low` — variación < 0.05 en cualquier variable

**observed_in_ags** indica si el patrón fue confirmado en la validación empírica del Nodo Aguascalientes (22–23 feb 2026).

---

## Recomendaciones activas

<div id="sf-recommendations" data-sf-load="true">—</div>

---

[Catálogo →](/docs/catalogo/) · [Dashboard →](/mihm/) · [Hub Nodo AGS →](/nodo-ags/) · [NTI →](/mihm/nti/) · [Patrones →](/mihm/patrones/)